/* eslint-disable @typescript-eslint/no-floating-promises */
import assert from 'node:assert'
import { describe, it } from 'node:test'

describe('should run the script', () => {
    it('scripting test', () => {
        console.log('run')

        assert(true)
    })
})
